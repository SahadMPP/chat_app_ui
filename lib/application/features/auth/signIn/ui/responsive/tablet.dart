import 'package:flutter/material.dart';

class SignInTebPage extends StatelessWidget {
  const SignInTebPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}