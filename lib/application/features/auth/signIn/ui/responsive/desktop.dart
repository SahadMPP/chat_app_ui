import 'package:flutter/material.dart';

class SignInDeskPage extends StatelessWidget {
  const SignInDeskPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}